# Public Support Matrix

The normative matrix is `policy/support-matrix.json`, validated against
`schemas/support-matrix.schema.json`. This document is a human summary and does
not broaden any machine-readable claim.

## Current truth

The project is `pre-alpha-contract-only`, and `supportClaimsEnabled` is `false`.
There is no `supported` input, OOXML feature, capability, platform, evidence
level, or delivery flow. D-037 adds one explicitly `experimental`, installed,
public-synthetic POTX-to-candidate flow; it does not enable global support
claims. The executable code currently in this repository protects admission
and validates this policy. It also validates the version `0.1.0` data-contract
shapes, creates ignored repository-owned synthetic PPTX/POTX test data, and
normalizes that reviewed lane's explicit parsed package view into a redacted
TemplateIndex. A separate internal lane now opens the `ProjectContext`-bound
source and validates the public minimal fixture through fixed filesystem,
ZIP/XML/OPC, relationship, and high-risk rejection controls. This is bounded
security evidence, not a published input or arbitrary-template compatibility
claim. A separate runtime-only dispatcher now atomically binds complete trusted
artifact registrations and executes a data-only conformance probe through
batch preflight, input/output schemas, support policy, and QA assertions. The
probe is not a product capability. M2-002 now resolves caller-supplied in-memory
registry/overlay/index/deck documents into private dispatcher invocations and
admits a real clone/fill product executor whose conformance output is a closed
JSON operation plan. The normative row remains `unsupported/unavailable`, so
normal product dispatch is blocked before preflight; no OOXML is cloned,
mutated, staged, or published. M2-003 adds one separately scoped experimental
capability: `native-card-arrow` emits an explicitly unbound, non-insertable
`p:grpSp` conformance exemplar containing native card and arrow shapes. This is
not an assembled PPTX or PowerPoint editability result. M2-004 adds an isolated
optional Pandoc adapter contract: an explicitly configured process must fall
inside the eligibility window and pass all active probes before it receives a
bounded Math JSON AST on stdin and has its DOCX stdout rebuilt as one canonical
unbound `m:oMath` exemplar. Public
tests use a fake runner and text-derived in-memory DOCX, so formula input,
formula transplantation, and native OMML all remain unsupported.

M2-005 now also contains a target-specific native application bridge, a
persisted semantic-slot `SlideLayoutIR`, a digest-bound frozen
`ComposedSlidePlan`, a slide-wide paint-bounds occupancy gate, and an explicit
non-deliverable candidate writer. The bounded direct-native paths can now write
the PPTX together with a closed `CandidateBuildRecord 0.1.0` sidecar that binds
the exact bytes while retaining both the input constraints and replayed plan.
One repository-owned synthetic candidate has local render and PowerPoint
edit/save/reopen evidence. A second bounded path now
strict-parses and typed-rebuilds one direct `a14:m -> m:oMath` target through
the same semantic slot, frozen plan, exact-diff, private-identity, and candidate
writer boundary. PowerPoint opened and round-tripped that synthetic candidate,
and one separately frozen mixed-candidate derivative records a real human
`3/3 -> 2/3` edit/save/close/reopen that remains editable. The corresponding
feature record includes an explicit no-repair/conversion attestation and passes;
it cannot substitute for the direct candidate's exact
`formula-content-edit-save-reopen` receipt. These are narrow
implementation and compatibility facts, not a published input/output flow;
the same record contract now covers complete generic, semantic-card, and typed-
OMML ordered decks by preserving exact slide order, per-slide source hashes,
deck diff, and every native slide's IR/plan replay. One smaller direct single-
slide native-card path now authenticates calculation, render,
independent-pixel, and local compatibility receipts for the exact candidate,
but only after the shared pure layout calculation re-derives and exact-compares
the complete IR/plan from source/index/DeckSpec facts, then deterministically
reconstructs the exact source-template → index/overlay binding → DeckSpec
payload → composed-plan PPTX and rejects any semantic or collateral mismatch.
It creates a passing `QaReport` and publishes `BuildArtifact` last. Its
cancellation path removes the reserved delivery directory before commit. A
second direct typed-OMML implementation now regenerates from the exact source
template and captured DeckSpec LaTeX, exact-compares the complete archive and
IR/plan/diff/formula evidence, and requires a purpose-bound
`formula-content-edit-save-reopen` compatibility receipt. The existing shape-
name round trip cannot satisfy that gate, so no actual OMML final delivery is
authorized by current repository evidence. A third preparer now implements the
same authority rule for clone/fill-only ordered candidates: it regenerates one
complete 2–11 slide same-template DeckSpec batch, exact-compares the rebuilt
archive and all ordered record facts, requires full ordered slide coverage in
mechanical/render receipts, and purpose-binds compatibility to
`ordered-deck-open-save-reopen`. An optional trusted local adapter now generated
one exact two-slide public-synthetic candidate and record, preserved the source,
and recorded PowerPoint 16.111.3 opening, exporting, saving, closing, reopening,
and re-reading the same ordered slide/text inventory. Its before/after
PowerPoint PDF pages rasterized to byte-identical 1600 × 900 pixels, and one
independent pixel-only review found no clipping, overlap, wrapping, missing
text, or order drift. Eleven is still the last executable package size under
the unchanged 32-entry secure-ZIP profile; 12 slides fail before dispatch or
artifact inspection. This is not a public output-flow claim: the evidence covers
one exact local two-slide synthetic candidate only. Native/OMML ordered final
authority, a complete fault/compatibility matrix, real-Pandoc evidence, the
direct-candidate formula-content receipt, arbitrary-template evidence, and
support remain absent.

D-033 closes the M2-005 implementation milestone only for the exact fixed mixed
public-synthetic vertical slice described above. That scope decision changes no
row: every unproven path remains unavailable, all published support counts stay
unchanged, and `supportClaimsEnabled` remains `false`.

D-034 adds a public CLI protocol and the first real `inspect`/document-validation
orchestration over the already-bounded public-synthetic input lane. D-036 adds
an explicit public-synthetic bootstrap plus whole-project validation. D-037
adds the exact `render` candidate path described below. `doctor`, generic
`onboard`, standalone `diff`, and the formula CLI remain unavailable. D-038
adds `qa` only for the exact D-037 candidate and deliberately returns a blocked
report with no `BuildArtifact`; inspection, preflight, that candidate pair, and
blocked QA are not arbitrary-template support or delivery authority.

D-023 makes rendered communication effect the product objective and permits
optional planning/review experiments under `labs/`. This changes no row below:
the labs are non-authorizing, core cannot import them, their current schemas and
reports are not public capability contracts, and model judgment is not audience
effect evidence. `supportClaimsEnabled` remains `false`.

| Dimension | Items | Supported | Experimental | Manual | Unsupported |
| --- | ---: | ---: | ---: | ---: | ---: |
| Inputs | 13 | 0 | 5 | 0 | 8 |
| OOXML features | 22 | 0 | 5 | 0 | 17 |
| Capabilities | 8 | 0 | 5 | 0 | 3 |
| Platforms | 10 | 0 | 6 | 1 | 3 |
| Evidence levels | 7 | 0 | 1 | 2 | 4 |
| **Total** | **60** | **0** | **22** | **3** | **35** |

The six experimental platform rows are the declared Node.js 22.x/24.x runtime
envelope on Linux, macOS, and Windows. The public-automated evidence level and
the exact D-037 closure account for the other experimental rows. Platform rows
remain experimental until hosted M3-004B cross-platform CI exists after M3-006;
only the runtime row matching the executing platform and Node major can
authorize this candidate. Local M3-004A workflow definitions and macOS runs do
not promote a platform row.

## Status vocabulary

- `supported`: the exact scope has a dispatchable implementation and all
  required public conformance evidence. A capability additionally needs
  metadata, executor, input/output schemas, conformance fixture, and QA
  assertions.
- `experimental`: a bounded implementation path or contract exists, but its
  stability or coverage is incomplete. It must remain labeled at all public
  boundaries. Capability rows still require the same six executable artifact
  classes as supported capabilities and explicit per-selection opt-in.
- `manual`: an explicit trusted human or external-runtime gate is required.
  The result remains unresolved until recorded and never becomes an automatic
  pass.
- `unsupported`: reject or report unavailable before output mutation. There is
  no silent fallback, preservation guess, or flatten-to-image escape hatch.

Roadmap entries describe intended work only. They do not change current status.

## Important boundaries

- The exact generated public-synthetic POTX input is experimental; PPTX input
  and arbitrary POTX templates remain unsupported. `inspectTemplateSource()`
  and D-037's stable-snapshot API prove only the fixed narrow source-to-index
  and candidate path; no arbitrary template class is promised.
  The direct semantic inspector still performs no archive/XML I/O and a caller
  package-view label still proves no origin.
- ProjectConfig is experimental only inside the exact D-037 profile. It has
  automated schema evidence, a pure in-memory
  `ProjectContext`, one bounded contained CLI JSON reader, and an accepted exact
  public-synthetic whole-project preflight and candidate path. Every other
  ProjectConfig scope remains unavailable; candidate success grants neither
  delivery eligibility nor global support promotion.
- Profile/index has automated schema evidence, a reviewed-fixture semantic
  inspector, a bounded secure-ingestion producer, and a pure in-memory
  overlay/index/deck resolver. D-036 loads Profile, Index, Registry, Overlay,
  and DeckSpec only for an explicitly labeled public-synthetic conformance
  project, re-inspects its source, performs normative closure authorization,
  and prepares one fresh one-shot native-card dispatch. That narrow loader is
  not an arbitrary project input claim. The clone/fill
  executor produces only semantic operation plans and has no staging authority
  or render flow. A separate bounded core path can apply those plans to the
  repository-owned synthetic source and now regenerates a same-template 2–11
  slide batch for receipt-bound ordered preparation; it is not a public loader,
  CLI, renderer, or compatibility claim. Schema-only fixtures remain explicitly excluded from product
  capability conformance evidence.
- `native-card-arrow` accepts only closed geometry/text/style data and one exact
  text-box anchor. Its canonical unbound exemplar remains `insertable: false`.
  The narrow M2-005 bridge now rebuilds it with collision-free target IDs and
  exact one-slide diff. Product-level placement uses a bounded semantic slot and
  frozen plan; the old x/y input remains only a low-level capability primitive.
  The create-only bundle writer persists those constraints and resolved facts
  in a canonical, replay-validated, PPTX-hash-bound sidecar; its PPTX-only
  predecessor remains compatible. A separate direct final boundary can now
  grant delivery authority only when trusted mechanical, same-candidate render,
  independent-pixel, and PowerPoint compatibility receipts all pass; it reuses
  `QaReport` and `BuildArtifact` and rejects ordered candidates. A separate
  direct typed-OMML preparer uses the same publisher only after exact source/
  DeckSpec regeneration and an operation-specific formula-content receipt. A
  bounded clone/fill-only ordered preparer reuses that publisher after complete
  source/DeckSpec batch regeneration and exact ordered-record comparison. One
  ignored local `ordered-deck-open-save-reopen` receipt now exists for the fixed
  two-slide public-synthetic candidate, with readable text/order and identical
  before/after PowerPoint pixels; it does not generalize to the 2–11 executable
  range, other templates, or target-specific native/OMML members.
  General
  DrawingML, general slide text, complete PPTX rendering, arbitrary-template
  editability, and a public output flow remain unavailable.
- The formula adapter accepts only one bounded in-memory display-math payload
  and one exact text-box target binding. Pandoc remains a user-installed,
  explicitly configured external process; missing, pre-2.15, future-major, or
  capability-drifting binaries produce no executor registration. Formula data
  is JSON-encoded inside a Pandoc Math AST and never enters argv or a path. The
  resulting `m:oMath` is strict-parsed, allowlisted, canonically rebuilt, and
  marked `insertable: false`. The bounded M2-005 applicator now independently
  reparses that result and typed-rebuilds one synthetic target with exact slot,
  plan, capacity, and collateral evidence; this does not make the adapter output
  insertable or broaden normal dispatch. The exact target constraints and
  formula evidence can be written beside direct and ordered candidates in the
  same normative sidecar contract. Ordered assembly preserves exact authentic
  formula slide bytes and the full per-slide IR/plan/diff/evidence tuple without
  adding a second semantic model. The direct final-preparation path is executable
  but cannot activate from the existing shape-name receipt or the separate
  mixed-candidate feature observation. That observation now proves only the
  readable `3/3 -> 2/3` change, continued editability, and the operator's no-
  repair attestation; it grants no delivery authority. A compatible
  real-Pandoc public matrix, an exact direct-candidate receipt, broader templates,
  and support review are still required.
- Macros, ActiveX, OLE, embedded packages, and external relationships have
  executable rejection mutations in the bounded ingestion lane. Embedded
  fonts, unknown vendor XML, and every unlisted OOXML feature also retain a
  `reject` disposition; none of these rejection results is compatibility or
  capability support.
- Native charts, tables, arbitrary animations, media, notes/comments, and
  digital-signature handling have no 0.x support promise unless a later matrix
  revision says otherwise.
- PowerPoint automation is a manual trusted lane only. It is optional, absent
  from public PR CI, and cannot replace public synthetic conformance. The one
  passing public-synthetic native save/reopen probe is local compatibility
  evidence only and changes no matrix status.
- The design-planning and visual-review labs cannot register executors, select
  support dispositions, mutate packages, or satisfy a capability row. A future
  public visual-review capability requires its own schema, producer, synthetic
  conformance, deterministic verdict boundary, delivery-mode controls, blind
  outcome evidence, and explicit non-audience-study limitation.
- Predecessor observations, compilation success, screenshots, and private
  fixture results cannot independently establish public support.

D-035 changed planning only and authorized no row at that time. D-037 now
satisfies its candidate prerequisite: the exact native-card dependency closure
is enumerated and every exercised invariant row is promoted together to
`experimental` with explicit opt-in while `supportClaimsEnabled` remains false.
If any required input, OOXML, capability, output, evidence, or runtime row is
missing or has the wrong disposition, render stops before assembly. No
conformance-only resolver substitutes for the normative matrix. Missing later
render/pixel/compatibility evidence now produces the fixed blocked QaReport and
still means no BuildArtifact or delivery authority.

D-036 satisfies only the pre-render project-spine dependency. Its static host
uses the existing `native-drawingml` experimental row and requires explicit
opt-in; it neither changes the 60-row counts nor checks the complete future
render dependency closure. Machine output says registration conformance passed,
the project invocation was not run, and render eligibility was not granted.

D-037 closes exactly one installed public-synthetic native-card candidate path.
Its frozen profile owns 15 invariant dependency rows; the static host also
requires the one runtime row matching Linux/macOS/Windows and Node 22/24 plus
the `automated-public-synthetic` evidence row. It exact-compares the fresh
TemplateIndex, derives geometry from the semantic `slide-content-tail` slot,
executes one fresh dispatch plan, verifies occupancy and exact package diffs,
then publishes a replayable record first and the PPTX commit marker last. The
result is labeled `experimental`, `deliveryEligible=false`, and creates neither
QaReport nor BuildArtifact. Every other row remains fail-closed.

All owned regular-file payloads are flushed before that logical marker on every
admitted platform. POSIX additionally requests parent-directory fsync barriers.
Windows retains create-only visibility, record-first/marker-last ordering,
exact reconciliation, and the same fault-state machine, but has no sudden-
power-loss directory-entry persistence claim.

D-038 closes only the honest assessment over that exact candidate. The same
static profile additionally requires `manual-trusted-runtime` and
`macos-powerpoint-automation` to remain `manual/report-manual-gate`; these rows
describe unavailable evidence and do not become satisfied dependencies. QA
re-derives the current expected PPTX and canonical record, exact-compares both
stable-read files, verifies record replay, and emits the existing QaReport with
three structural checks passing and three external checks unavailable. The
decision is `blocked`, no BuildArtifact is created, ignored local evidence is
not read, and all 60 row statuses plus `supportClaimsEnabled=false` remain
unchanged.

## Verification

Run:

```sh
npm run check:support-matrix
npm run check:contracts
node --test tests/capability-dispatcher.test.mjs
node --test tests/source-slide-clone-fill.test.mjs
node --test tests/native-card-arrow.test.mjs
node --test tests/native-card-arrow-assembly.test.mjs
node --test tests/cli.test.mjs
node --test tests/pandoc-omml-adapter.test.mjs
node --test tests/receipt-bound-final-delivery.test.mjs
node --test tests/receipt-bound-omml-final-delivery.test.mjs
node --test tests/receipt-bound-ordered-final-delivery.test.mjs
```

The gate reads the staged matrix and schema by default, validates their
structure and semantic status combinations, requires catch-all rows, checks
evidence paths against admitted files, and rejects overclaims. Use
`--mode working-tree` only for pre-staging diagnostics.

The contract gate separately validates the staged `0.1.0` schema manifest,
positive examples, cross-document semantics, and generated TypeScript. Passing
it does not alter any support status.

The dispatcher test verifies runtime admission and execution mechanics using a
separate conformance-only probe. The clone/fill test verifies exact semantic
resolution and product artifact conformance, then separately proves that the
normative unavailable decision blocks normal dispatch. The native-component
test verifies the exact experimental fragment boundary and explicit opt-in;
passing it establishes neither target-package application nor a manually
editable PowerPoint file. The formula-adapter test verifies detection,
process/resource isolation, DOCX/OMML reconstruction, and dispatcher
conformance with a fake runner; it is not compatible-Pandoc or PowerPoint
support evidence.
