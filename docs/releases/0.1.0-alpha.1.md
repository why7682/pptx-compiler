# PPTX Compiler 0.1.0-alpha.1

> **Status boundary: release-state neutral.** This is the immutable
> release-facing projection for candidate version `0.1.0-alpha.1`; it neither
> announces nor denies mutable external release state. Verify that state only
> by joining the exact annotated tag and tracked lock with official npm
> bytes/provenance/dist-tags and the GitHub Release record. D-048 is
> authorization, not execution evidence.

## Purpose and boundary

This candidate alpha demonstrates one trustworthy, installed path from a
repository-owned synthetic POTX project to a native DrawingML card-arrow
candidate PPTX with a replayable build record and honest blocked QA. It is intended for
evaluating the contracts, failure behavior, package boundary, and contributor
workflow—not for general presentation production.

The public path does not yet provide the PowerPoint compatibility evidence
needed to claim editability or delivery readiness.

It is not arbitrary-template compatibility, a general renderer, delivery
approval, or a claim that any support row is `supported`.

## Fact ownership

The source template owns unchanged inherited content. The project JSON data and
`DeckSpec` own authored meaning. `SlideLayoutIR` owns editable constraints;
`ComposedSlidePlan` owns resolved geometry. `CandidateBuildRecord` binds both to
the exact candidate bytes. `QaReport` records the assessment, and only a later
passing final-delivery boundary could create `BuildArtifact`.

The support matrix owns claim status, the package plan owns intended tarball
contents and publication settings, and the release gates own phase order and
publication eligibility. This note is only a lock-bound projection of those
authorities; it never owns current registry or GitHub state.

## What the candidate contains

The candidate release graph has four packages:

- `pptx-compiler`: the explicit-root CLI;
- `pptx-compiler-core`: contracts, secure ingestion, dispatch, candidate
  assembly, replay, and QA foundations;
- `pptx-compiler-native-card-arrow`: the exact native DrawingML capability;
- `pptx-compiler-public-synthetic`: the repository-owned conformance project.

The workspace root remains private. Historical M3 leaf manifests and tarballs
are also private package-boundary evidence and are not release bytes.
Package-plan schema 3 defines the authorized public manifests. The M4-001 lock
requires equal canonical tar payloads from both builders and alone identifies
the fixed Node 24/npm 11 gzip envelopes as candidate release bytes.

## Candidate flow

The installed smoke path is:

```text
init public-synthetic project
  -> inspect exact bounded POTX
  -> validate complete project graph and experimental support closure
  -> render one native-card candidate and CandidateBuildRecord
  -> qa re-derives and compares both artifacts
  -> decision: blocked; no BuildArtifact
```

The renderer requires explicit experimental opt-in. It derives placement from a
semantic slot, preserves source bytes, verifies containment and collateral
diff, writes the record before the PPTX logical commit marker, and never turns
the candidate into a delivery merely because assembly succeeds.

## Executable contracts

- JSON Schema is normative for project, template, capability, deck, candidate,
  QA, and build records; generated TypeScript declarations are checked public
  consumers.
- Unknown versions, fields, inputs, features, relationships, semantic bindings,
  platforms, and evidence fail closed.
- The exact capability registration binds metadata, executor, schemas,
  conformance cases, and QA assertions before any invocation can run.
- Candidate generation and QA use the same pure layout derivation and compare
  the complete intent, plan, package diff, record, and PPTX bytes.
- Publication failures expose not-committed, rollback-incomplete, or
  commit-uncertain states instead of hiding ambiguity.

## Public evidence

The public repository records the following hosted evidence:

- [PR Public CI 31600528716](https://github.com/why7682/pptx-compiler/actions/runs/31600528716):
  all Linux/macOS/Windows × Node 22/24 cells passed;
- [PR Security 31600528742](https://github.com/why7682/pptx-compiler/actions/runs/31600528742):
  Dependency Review passed;
- [accepted-main Public CI 31600806512](https://github.com/why7682/pptx-compiler/actions/runs/31600806512):
  all six cells passed for object `c4dee58`;
- [accepted-main Security 31600806350](https://github.com/why7682/pptx-compiler/actions/runs/31600806350):
  CodeQL passed for the same object.

The canonical workflow runs source, provenance, forbidden-material, contract,
support, type, import, package-plan, complete-test, guarded-tarball,
empty-install, installed-CLI, and tracked-drift checks. Private templates,
PowerPoint, Pandoc, models, credentials, signing material, publication tokens,
and ignored local evidence are absent.

This evidence closes the hosted M3-004B/G5 prerequisite. It does not prove that
an annotated tag or registry package equals the reviewed artifact.

Constructive-document PR #2 head
`093d527fc3fadf7cae577139b8d400719755dd52` passed Public CI run
`31608992503` and Security run `31608992491`; accepted main
`8cdf968b72f8dd5f41fee37a68e239e477dec44b` passed Public CI run
`31609285181` and Security run `31609285220`. Both share tree
`1d6d148a8bc347dc3cbc13dde3fd4314d86c421a`. This is the accepted reader-
document baseline entering M4, not clean-tag, public-package, registry, or
provenance evidence.

## Support state

The matrix remains:

| Supported | Experimental | Manual | Unsupported |
| ---: | ---: | ---: | ---: |
| 0 | 22 | 3 | 35 |

`supportClaimsEnabled=false`. The six platform rows now carry automated public
CI evidence but remain experimental, because a passing platform cell does not
authorize a capability or delivery flow.

## Limitations

The authoritative user-facing list is
[`docs/KNOWN_LIMITATIONS.md`](../KNOWN_LIMITATIONS.md). The release-critical
limits are:

- only the fixed public-synthetic POTX/native-card candidate profile is exposed;
- QA is intentionally blocked and produces no `BuildArtifact`;
- PPTX input, arbitrary POTX, formula delivery, asset placement, charts, tables,
  media, animations, notes, and comments are outside scope;
- desktop PowerPoint and real Pandoc are not public-CI dependencies;
- Windows has no sudden-power-loss directory-entry persistence claim;
- this note does not establish npm distribution, tag identity, npm Sigstore
  provenance, or published-artifact equality; those facts require the external
  records joined to the tracked lock.

## Reproduction

Follow [`docs/REPRODUCIBILITY.md`](../REPRODUCIBILITY.md) from a clean checkout
with Node.js 22.x or 24.x and its bundled npm. The expected installed smoke
result is blocked QA and no `BuildArtifact`. Generated fixtures, tarballs,
smoke projects, decks, renders, and raw reviews remain ignored.

## Publication status and next authorized action

This note does not own publication status. For any incomplete M4-001 attempt,
take only the earliest unsatisfied transition: freeze inputs; require Node
22.23.2/npm 10.9.8 and Node 24.19.0/npm 11.17.0 to produce one four-package
lock; review, commit, and merge that lock as GitHub-verified commit `S`; append
exactly one repository-local identity attestation `A` whose sole parent is `S`
and whose tip policy grants `S`; pass the complete history gate at `main=A`;
then create annotated tag `v0.1.0-alpha.1` on unchanged `S`.

Tag-triggered Public CI and Security must pass before publishing core,
native-card-arrow, public-synthetic, and CLI in dependency order.
Official-registry bytes and npm provenance must equal the lock, `alpha` must
point to this version, `latest` must not, and the GitHub Release is created
last. Partial publication remains incomplete, and no step broadens support or
the separately deferred D-047 branch-protection scope.
