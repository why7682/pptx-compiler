# Codex Handoff

## Current state

This is an independent, clean-history workspace created to extract a generic
PPTX rendering and QA pipeline from a private predecessor project. It now has
plans, versioned data contracts, dependency-free foundation policy gates,
reviewed text-only OOXML fixture parts, generated TypeScript declarations, a
pure explicit-root runtime context, a pure semantic inspector, bounded secure
ZIP/XML ingestion for the public minimal fixture class, a fail-closed capability
runtime/dispatcher, a bounded pure project-dispatch resolver, a no-I/O semantic
clone/fill operation planner, an opt-in experimental native card-arrow planner
with a strict unbound DrawingML conformance exemplar, an isolated optional
Pandoc process adapter that emits only an unbound OMML conformance exemplar,
a bounded create-only one-slide clone/fill PPTX backend, an isolated calibrated
visual-review contract, a clean-room brief-to-layout selection lab, and
synthetic mutation tests. No arbitrary PPTX/POTX compatibility layer, broad
package mutation implementation, tracked binary fixture, design asset,
runtime dependency, prompt log, or predecessor Git history has been copied. The
private root now has one reviewed dev-only TypeScript lock entry for public
declaration checking; it is outside the four-package runtime closure.

The project uses the MIT License with Elliot Wu as the declared copyright
holder. D-044 selects `pptx-compiler`, the exact public target
`why7682/pptx-compiler`, the monorepo shape, and the repository-local public Git
author identity. Before every commit, run the leakage checks defined in
`docs/RELEASE_GATES.md`.

`M0-004` is complete: the repository now has dependency-free, fail-closed
forbidden-material and exact per-file provenance gates. The bounded suite has 2
passing baselines and 31 rejection mutations. Its independent read-only review
closed with no blocking findings.

`M0-005` is complete: the versioned public support matrix covers 60 input,
OOXML, capability, platform, and evidence items. Its global claim switch is
false, so no PPTX capability is currently represented as supported. A
dependency-free staged/working-tree gate rejects matrix overclaims and drift.

`M1-001` is complete: 12 independently reviewed, repository-owned text parts
generate deterministic ignored PPTX and POTX archives through a dependency-free,
create-only ZIP writer. The suite locks the minimal internal graph, content
types, owner references, source limits, high-risk exclusions, ZIP metadata, and
publication behavior. This is public test-data production, not PPTX ingestion
or rendering support; the global support switch remains false.

`M1-002` is complete: eight closed root schemas plus shared definitions form
contract version `0.1.0`. A dependency-free staged gate validates registered
references, positive examples, cross-document keys/bindings, safe relative
paths, QA/manual aggregation, and generated TypeScript drift. The example
bundle is machine-marked `schema-conformance-only`; its placeholder executor
references and fictional build/QA records are not runtime or support evidence.
The 146-test suite passes on Node.js 22.23.1 and 24.14.0; cross-platform OS
evidence remains M3-004B.

`M1-003` is complete: `ProjectContext` revalidates a detached frozen
`ProjectConfig 0.1.0` snapshot through an explicit synchronous dependency and
resolves eight locations beneath an explicit absolute root. It rejects lexical
escape, overlapping/case-aliased roots, all 10 case-only input-document role
aliases, and source/write conflicts without filesystem I/O or ambient root
discovery. The 225-test suite passes on Node.js 22.23.1 and 24.14.0. This is path
modeling, not template inspection or symlink/TOCTOU-safe input opening.

`M1-004` is complete: the dependency-free semantic inspector consumes an
explicit `ProjectContext`, a bounded already-parsed reviewed-fixture package
view, and an injected exact `TemplateIndex 0.1.0` validator. It emits the
deterministic path/text-redacted public golden, preserves source owner/z-order,
and rejects graph ambiguity, unsupported vocabulary, unbounded arrays,
accessors, and unknown fields. The reviewed producer fingerprints its normalized
OOXML structure so unmodeled attributes and children cannot disappear into an
empty feature list. The 344-test suite passes on Node.js 22.23.1 and 24.14.0;
the independent closing review reports no remaining blocker. This is not secure
ZIP/XML ingestion, arbitrary-template support, or npm clean-install evidence.

`M1-005` is complete: the high-level inspector derives its source only from a
frozen `ProjectContext`, takes one bounded stable file snapshot, validates a
fixed non-extracting ZIP profile, parses a namespace-aware bounded XML subset,
and maps only the public minimal OPC/PresentationML vocabulary. Traversal,
symlinks, archive bombs and ambiguity, malformed XML, external relationships,
macros, ActiveX, embedded objects, and unknown vocabulary fail closed. The
422-test suite passes on Node.js 22.23.1 and 24.14.0; the bounded closing review
reports no remaining blocker or high finding. The portable filesystem race
residual is documented, and no input or capability support claim was promoted.

`M2-001` is complete: declarative capability metadata becomes executable only
through an atomically admitted trusted registration with exact executor,
input/output schemas, nonempty conformance fixtures, QA assertions, and support
binding. Dispatch snapshots and preflights the complete batch before execution,
uses an authenticated one-shot plan, and validates bounded outputs plus QA. The
85-node dispatcher suite and complete 507-test repository suite pass on Node.js
22.23.1 and 24.14.0; dispatcher plus support-matrix tests pass 99/99 under
`--unhandled-rejections=strict`. The bounded closing review's
registration-race blocker and two resource/rejection-handling high findings
were fixed and rechecked with no remaining blocker or high. The only executor
is a data-only conformance probe; all 60 product support rows remain
non-supported and the global switch is false.

`M2-002` is complete: the runtime authenticates exact captured registry content,
and the synchronous resolver snapshots and validates the complete
registry/overlay/index/deck bundle before resolving every semantic binding and
preparing any executor preflight. The statically registered clone/fill plugin
accepts only two exact text-box roles from one source slide and returns a closed,
schema-validated data-only operation plan guarded by four QA assertions. The
56-node M2-002 suite, 156-node resolver/dispatcher/support focus, and complete
564-test repository suite pass on Node.js 22.23.1 and 24.14.0. The closing
review's provenance, pre-encoding resource, Unicode-contract, and stale-policy
findings were fixed, with no remaining code blocker or high finding. The plan is
not OOXML mutation, rendering, editability, staging, source-isolation,
collateral-diff, or publication evidence. All 60 support rows remain
non-supported and the global switch is false.

`M2-003` is complete: the data-only `native-card-arrow@0.1.0` capability follows
the same resolver/dispatcher path and canonically emits one strictly allowlisted
native `p:grpSp` containing a text-bearing `roundRect` and a `rightArrow`. Its
typed construction, safe text/color/geometry limits, local-ID policy, exact
schema validation, and native-vocabulary QA fail closed. The output is an
unbound conformance fragment with `insertable: false`; M2-005 must rebuild it
against a target slide, allocate collision-free IDs, apply it in create-only
staging, and prove PowerPoint editability. The 57-node capability suite,
215-node resolver/dispatcher/clone-fill/support focus, and complete 623-test
suite pass on Node.js 22.23.1 and 24.14.0. One narrow capability row is
experimental and opt-in, all broader DrawingML/input/package claims remain
unsupported, no row is supported, and the global switch remains false.

`M2-004` is complete: a core-isolated optional adapter accepts only a trusted
absolute path to a separately installed Pandoc process, then requires bounded
version, sandboxed JSON-API, and exact canonical-OMML probes before a formula
executor can exist. One closed display-math payload and one semantic text-box
target travel through the existing resolver/dispatcher; formula data appears
only inside a JSON Math AST on stdin. Bounded DOCX stdout passes the secure ZIP,
strict XML, content-type/relationship, exact Word wrapper, and typed OMML
topology gates before canonical reconstruction. The result is an unbound
`m:oMath` conformance fragment with `insertable: false`, not a PPTX insertion or
PowerPoint editability result. Public tests use a fake runner and text-derived
in-memory DOCX; all formula rows remain unsupported and the global switch is
false. D-008 and the fixed 3.10.1 attribution audit keep Pandoc unbundled and
record the GPL-2.0-or-later/generated-output boundary. The 115-node adapter
suite, 330-node strict dispatcher/resolver/native/formula/support focus, and
complete 738-test suite pass on Node.js 22.23.1 and 24.14.0. The bounded closing
review's relationship-profile high and content-type medium were fixed with
exact mappings and 10 regression subtests; re-review found no remaining blocker
or high finding.

`M2-005` is complete for the exact fixed public-synthetic mixed vertical slice.
`M2-005A` revalidates one exact public synthetic
POTX, re-derives its one-master/one-layout/one-slide index and source-order shape
facts, consumes one upstream-authenticated frozen clone/fill plan, rebuilds two
target text nodes, converts the main content type to PPTX, proves an exact
two-part allowed diff, creates deterministic archive bytes, re-ingests them,
and publishes create-only through same-directory staging. Archive limits apply
before input copying and seven focused assembly tests pass.

The bounded `M2-005B` ordered-story slice now captures one complete reviewed
`setup → evidence → resolution` batch, selects three authentic source
hierarchies, binds every one-slide report to its exact bytes, requires every
content part to belong to the exact typed relationship closure, and allocates
fresh deterministic slide parts, presentation IDs, and relationship IDs. The
first render review exposed generic evidence wording; after repairing the brief,
three fresh 1600 × 902 reviews judged the goal met with no finding and the
executable aggregate passed. Fourteen ordered-story tests and the complete
804-test repository suite pass; the bounded closing code review found no
remaining blocker, high, or medium issue. See
`docs/M2-005_ORDERED_STORY_DECK.md`. This still does not satisfy native/OMML
application, rollback, schema/contract integration, PowerPoint editability,
compatibility, or full milestone evidence.

The visual reviewer under `labs/visual-review-agent/` was empirically calibrated
with three independent reviews over two rendered public synthetic candidates.
Its closed report validator and executable three-run aggregator enforce 2-of-3
ordinary findings plus context- and crop-bound blocker follow-up; thirteen
focused tests pass. That `0.1.0` contract remains valid for hierarchy,
readability, visible defects, and basic goal alignment, but it is no longer
treated as a design-excellence gate. A
commit-pinned implementation audit of OfficeCLI, ppt-master, PPTAgent, the AWS
spec-driven maker, Presenton, pptx-automizer, docxtemplater, and pptxtojson then
corrected the sequence: the fundamental missing product layer is an audience-
goal/takeaway/evidence brief matched to template-derived semantic slots, not
broader OOXML coverage. `labs/layout-selection/layout-selector.mjs` implements
the first non-authorizing hard-fit/lexicographic prototype with explicit asset
inventory, per-kind capacity, deterministic per-layout/request budgets, and
fourteen focused tests. No external source or worktree entered this repository.
The bounded reviewer then closed every assembly, layout-selection, and visual-
aggregation finding; the final re-review found no blocker, high, or medium gap.

Reviewer `0.2.0` is now executable as a separate lab contract rather than an
extension of the old self-aware goal check. One fixed reviewed visual-evidence
manifest first binds the assembly, render set, anonymous contact sheet, and all
required-item source/crop evidence plus the reviewed whole-deck crop registry.
Three blind outcome reports and three label-blind whole-deck reports must then
validate and freeze under the same opaque one-shot session before the fixed
reviewed hidden key and atomic P1 outcome manifest can be revealed. The
instance lane evaluates every required claim,
action, boundary, and evidence item exactly once; matched items must cite their
exact visible blind slot, while one non-matched run creates a mandatory source-
and crop-bound reconciliation and cannot be outvoted. A
separate label-blind contact-sheet lane covers subject conditionality,
decorative-semantic fit, signature and motif coherence, silhouette and density
rhythm, opening/closing payoff, and delivery fitness. Ordinary whole-deck
findings need two independent spatially and slide-compatible runs, while every
blocker is bound at freeze time to one fixed reviewed crop before follow-up.
Spatially incompatible concern
votes stay unable-to-judge instead of inventing a repair, and caller-supplied
simulated dismissals cannot authorize pass. Delivery profiles distinguish
live-room, leave-behind, and hybrid use; no universal word, element,
whitespace, or font-size quota remains in the `0.2.0` prompt. Eleven text-only
calibration controls, twenty-three focused positive/adversarial tests, and the
complete 842-test repository suite pass. All results remain
`simulated-review-only`; this is not actual audience-effect, rendering, or
product-support evidence. The bounded closing reviewer independently reproduced
the failed-freeze and post-reveal replay rejections, fixed-crop binding,
assessable-slide evidence rule, slide-scope consensus, and cross-session
identity rejection, and found no remaining blocker, high, or medium issue.

P3 is complete for the bounded public-synthetic experiment.
`labs/design-planning/controlled-comparison.mjs` derives all six causal/permuted
× generic/subject/stylized arms from one authenticated P1 selection, exposes
twelve byte-identical live/leave delivery review cells, randomizes labels and
three reviewer orders, and hands the complete matrix across one authenticated
all-arm boundary. `controlled-render-harness.mjs` writes six decks, eighteen
one-slide inputs/renders, and six complete contact sheets into one explicit
create-only root; exact inventory is checked before the manifest, partial or
extra roots fail, and stable
reuse rechecks exact batch bindings, paths, inventory, hashes, and dimensions
without rerendering. Quick Look and ImageMagick are optional local tools;
PowerPoint was not launched.

The final render holds all three visible text nodes identical across every
visual triplet. Three isolated blind reviews, after exact-image reconciliation
and explicit retraction of two incorrect blank/clipping observations, ranked
all three causal arms above all three action-before-boundary permutations. The
subject-grounded causal arm ranked first live three times and first, first, and
second for leave-behind; the stylized control remained competitive for reader
delivery. The remaining effect gap is an editable visual explanation of `3/3`
and `87%`, which now guides M2-005C native-component work. The final 1600 × 902
run is bound
by matrix receipt
`65009c3b41bc1678bd7bc36ebfab3ec8b93524e99fecea2041995398eb58c490`
and render manifest
`e11ab84da264763899d8526bef3ed3383e174867492c35fa57f12a85347c1d10`.
Seventeen focused and 859 complete tests pass. Generated artifacts and raw reviews
remain ignored; evidence is `simulated-review-only`, not actual audience,
PowerPoint editability, arbitrary-template, or support evidence.

The first M2-005C native-application sub-slice is now executable.
`packages/core/src/native-card-arrow-assembly.mjs` accepts only one authentic
M2-005A clone/fill artifact plus one frozen, opaque, single-invocation dispatcher
plan. It consumes that plan inside the assembly boundary, ignores the unbound XML
exemplar, revalidates the source slide and direct text-box anchor, checks the
typed component against the actual slide canvas, scans the complete root and
nested `p:spTree` object-ID space, and rebuilds one fixed native card-arrow group
with the three smallest collision-free positive UInt32 IDs. On the public
synthetic target those IDs are `4/5/6`; the group is inserted immediately after
the authenticated anchor, adds no relationship, and changes only
`ppt/slides/slide1.xml`. Twenty-four focused tests pass.

At that boundary the complete repository suite passed 883/883. A bounded independent review found
one medium pre-limit byte-copy issue in the exported ID allocator; the allocator
now sends the original byte view through the strict 256 KiB XML gate before any
copy, an oversized-input regression passes, and targeted closure found no
remaining blocker, high, or medium finding.

The immediately following authenticated bridge is now executable without
widening the group grammar. Native reports are registered in a private identity
map only after target-specific verification; ordered assembly accepts either an
authentic M2-005A report or that exact native report, copies the real native slide
bytes, and validates the rebuilt package through an in-memory shadow in which
only authenticated native slides are replaced by their authentic M2-005A bases.
The shadow goes through the unchanged generic package view and relationship-
closure checks. A separate create-only publisher accepts only a direct authentic
native artifact or a freshly registered ordered artifact containing at least one
such native slide. It snapshots before filesystem I/O and retains staging,
hard-link, no-overwrite, parent-identity, and cleanup rules.

`publicationEligible: false` remains correct for a direct native report: it says
the generic raw-byte publisher is unavailable, not that the private authenticated
bridge is unavailable. Caller labels never authorize the bridge. The real native
package and native-containing ordered package therefore remain rejected by the
generic package view and publisher. Thirty-six focused tests and the complete
895/895 repository suite pass. The bounded bridge review's one high hash-before-
copy TOCTOU was fixed with intrinsic length admission plus copy-then-hash of the
returned snapshot; direct and ordered SharedArrayBuffer regressions pass, and
targeted closure found no remaining blocker, high, or medium finding. No generic
ingest grammar, serialized public contract, or support row was expanded.

The native overlap/refactor closure is now executable. The old overlapping
geometry is a required failure; `SlideLayoutIR` preserves semantic slot bounds,
container padding, alignment, preferred size, paint outset, z-order, and
constraints, while `ComposedSlidePlan` separately freezes EMU geometry and
binds deterministic containment/slot/occupancy receipts plus IR/plan digests.
The product path accepts `slotRef + placementIntent + preferredSize` and derives
x/y from authenticated template facts; the existing raw-coordinate applicator
remains a low-level compatibility primitive. An explicit writer labels the
structural result `candidate-pptx` with `deliveryEligible: false`; the legacy
publication-named API remains only for compatibility and cannot authorize final
delivery.

One exact repaired public-synthetic candidate passed deterministic occupancy,
Quick Look rendering, a pixel-only independent review, PowerPoint 16.111.3
group rename/save/reopen plus in-memory child editability, strict post-save
package inspection, a fresh post-save render, and a second pixel-only review.
Local PPTX, PNG, automation, and raw review evidence remains ignored. Eight
layout-IR tests, forty native tests, and the complete 918-test suite pass. This
is narrow compatibility evidence, not arbitrary-template or support evidence.

The compatible typed OMML target is now executable. One authentic M2-005A
artifact and one exact one-shot formula dispatch produce a target-specific
candidate in which the source text target is replaced by a strictly parsed and
typed-rebuilt `a14:m -> m:oMath` tree. Core never imports the Pandoc adapter and
never copies the unbound XML string into the slide. The target uses the same
semantic-slot `SlideLayoutIR`, digest-bound `ComposedSlidePlan`, occupancy gate,
exact one-slide diff, private artifact identity, and non-deliverable candidate
writer as the native path. Its build report carries the replayable constraints,
frozen geometry, formula digest, target capacity, and both layout digests; PPTX
bytes alone remain non-authoritative for later regeneration.

PowerPoint 16.111.3 opened the exact direct-OMML candidate without repair,
saved a shape-name edit, closed it, and reopened it with the native formula
choice still present. PowerPoint normalized the saved formula into
`mc:AlternateContent`, retaining native OMML while adding its own PNG fallback;
the generator itself emitted no image or fallback. PowerPoint's extracted
fallback was complete and unclipped, and an independent reviewer passed a
layout composite made from those exact saved pixels. That composite is not a
direct PowerPoint screenshot, and the shape-name edit is not the required human
formula-content edit. D-032 later passed a separate mixed-candidate feature
observation; this direct candidate still has no operation-bound content-edit
receipt.

Eleven OMML tests and the 67-test create-only/native/layout/OMML focus pass. The
bounded review found one high shared-memory snapshot race in the reused M2-005A
authentication boundary and one medium `slide-canvas` reserved-ID containment
bypass. Copy-before-hash authentication, a reserved root identity, a mandatory
slot-check cardinality assertion, and two regressions closed both; final review
found no remaining blocker, high, or medium issue. The complete suite passes
930/930.

The normative candidate-record slice is now executable. The additive ninth
root contract, `CandidateBuildRecord 0.1.0`, keeps `BuildArtifact` reserved for
QA-backed publication while preserving one direct semantic-slot candidate's
complete input IR, exact replayed plan and constraint receipt, package diff,
native-card IDs or typed-OMML target evidence, authentic base digest, and
candidate basename/length/SHA-256. The canonical loader re-runs the resolver
and rejects plan, source-profile, evidence, component-identity, filename, or
byte drift. The bundle writer captures the artifact pair once, pre-limits bytes
before copying, file-flushes both staged files, publishes the record first, and
publishes the PPTX last as the logical commit marker. POSIX additionally
requests parent-directory fsync barriers. Handled rollback
removes the candidate before the record. The old PPTX-only writer and legacy
publication-named alias remain unchanged. Fixed-absolute compatibility output
and ordered decks fail closed at the record-backed bundle boundary until they carry
complete replay data.

Three record-unit tests plus direct native and OMML bundle regressions are in
the complete 942/942 repository pass. The bounded independent closure review
found and verified fixes for cross-pair Proxy authentication, discriminated
profile drift, capability-reference drift, Unicode/component filename bounds,
pre-copy resource admission, directory-barrier handling, and rollback ordering and
failure handling; no blocker, high, or medium issue remains.

The ordered-record follow-on is now executable. Generic and native-containing
ordered artifacts use a separate bundle entry point over the same record-first/
candidate-last pair
publisher. The record binds the complete slide order, part/relationship/
presentation allocation, base and per-slide source hashes, deck diff, and
candidate identity. The discriminated per-slide source build is authoritative;
redundant native flags, counts, and ordered source-profile labels are omitted.
Clone/fill slides remain explicitly digest-bound; every
semantic native slide keeps its source base/part/diff, full bounded-slot IR,
exact plan/receipt, and allocated IDs. Low-level fixed-absolute native slides
fail before staging instead of receiving a fabricated replay record.
The candidate/contracts/native/OMML/ordered/support focus passes 166/166 and
the complete repository suite passes 945/945. The bounded D-028 review found
one medium schema/runtime classification drift; deleting the redundant ordered
profile/native flag/count fields closed it, and re-review found no remaining
blocker, high, or medium issue.

The first receipt-bound final delivery slice is now executable for one direct
single-slide semantic-card candidate. It reuses `QaReport` and `BuildArtifact`,
but first re-derives the exact source index/overlay anchor, runs source shapes,
canvas, and the readable DeckSpec request through generation's shared pure
layout calculation, and exact-compares the complete IR/plan. It then
deterministically reconstructs the source-template + DeckSpec payload +
composed-plan candidate.
The narrowed base preserves source-visible text, exact POTX conversion and every
non-target part; a plain/OMML PPTX relabeled by a replay-valid record and any
collateral part mutation fail, as does a replay-valid record that preserves the
native box while falsifying a fixed source constraint. Trusted mechanical/
render/independent-pixel/PowerPoint receipts then bind the exact candidate/
record pair, and `BuildArtifact` links last. The final focus passes 23/23 and
the complete repository suite passes 968/968, covering semantic/index/binding/
byte/evidence drift, resource budgets, blocked evidence, ordered rejection,
create-only/one-shot publication, pre-commit rollback, post-marker commit
uncertainty, and caller-owned/shared-memory snapshots. Closing independent
re-review found no remaining blocker, high, or medium issue.

The typed-OMML ordered follow-on is now executable without a second ordered
pipeline. One target-specific native profile table authenticates either the
existing semantic card or the typed formula artifact, copies the exact authentic
slide bytes into the caller's requested first/last position, and uses only that
artifact's separately authenticated M2-005A base in the temporary verification
shadow. The ordered `sourceBuild` union now preserves the OMML artifact/profile
tuple, complete `SlideLayoutIR`, replayed `ComposedSlidePlan`, exact one-slide
diff, formula digest, target capacity, and observed target facts. The deck base
digest remains the digest of the first ordered source artifact; no derived deck-
level native/OMML classifier was added. Three new public synthetic/adversarial
tests bring the candidate/contracts/native/OMML/ordered/support focus to 201/201
and the complete suite to 971/971. The bounded independent review found no
blocker, high, or medium issue. This grants only ordered candidate-build replay;
ordered delivery, OMML delivery, support, and the manual formula-content edit
remain closed.

The direct typed-OMML final-delivery follow-on now closes the semantic gap that
the ordered candidate work deliberately left open. A source-preserving
generation entry point starts from the exact admitted template rather than the
clone/fill compatibility artifact, preserves unchanged inherited title/package
content, and shares the same typed OMML mutation core. Final preparation
snapshots all caller-owned facts before the async boundary, resolves a fresh
one-shot formula dispatch from the exact readable registry/overlay/index/
`DeckSpec` tuple, regenerates the complete candidate, exact-compares its bytes
and full IR/plan/diff/formula evidence with the reviewed candidate record, and
recomputes the source-to-candidate part diff. A clone/fill OMML candidate with
undeclared title text and a candidate built from different LaTeX both fail
before receipts; a no-op compatibility call that converges to the identical
readable result is accepted rather than creating provenance-by-call-history.
Compatibility evidence must explicitly authenticate
`formula-content-edit-save-reopen`; a shape-name edit cannot authorize this
path. The old synchronous native-card preparation API is unchanged. Fifteen
OMML assembly tests, twenty-three native-card final tests, six OMML-final tests,
and the complete 978/978 suite pass. The one bounded independent review's
call-history ambiguity was resolved by the no-op convergence contract and
regression; re-review found no blocker, high, or medium issue.

This is an implemented receipt-bound path, not proof that its exact direct-
candidate compatibility receipt exists. D-032's later feature-level
`COMPAT-OMML-001` pass cannot satisfy that receipt. Real-Pandoc coverage,
target-specific native/OMML ordered final delivery, broader templates, and
broader delivery fault/compatibility evidence remain open;
`supportClaimsEnabled` remains false.

The first ordered final-delivery slice is now executable for clone/fill-only
decks. It accepts one exact 2–11 slide ordered candidate plus one captured
same-template project tuple, resolves and executes the complete readable
`DeckSpec` batch once, rebuilds every one-slide clone/fill artifact from the
admitted source/index, and feeds those fresh artifacts through the unchanged
ordered assembler. The complete ordered bytes and candidate-record deck facts
must converge before receipts are consulted. Mechanical and render receipts
must name every slide in exact order; compatibility must name
`ordered-deck-open-save-reopen`; `BuildArtifact` records every slide and is
still published last. DeckSpec content/order drift, target-specific native
source builds, incomplete evidence, confused-purpose compatibility, and caller
mutation after the async boundary fail closed. At that implementation boundary,
eleven focused tests and the combined direct/OMML/ordered final focus passed
55/55; the complete suite passed 990/990. The 11-slide ceiling is the last
package size that fits the unchanged
32-entry secure-ZIP profile; 12 slides fail before dispatch or artifact
inspection. That code slice alone did not provide an ordered PowerPoint receipt,
broaden the one-slide public-synthetic grammar, or authorize native/OMML mixed
ordered delivery. The global support switch remains false.

The actual ordered compatibility receipt is now present for one exact two-slide
public-synthetic clone/fill candidate. A trusted local macOS adapter generates
the candidate and CandidateBuildRecord in one stable ignored root, requires an
explicit local-run flag, refuses CI and pre-open user presentations, and invokes
only PowerPoint's static object model with absolute paths. PowerPoint 16.111.3
opened the candidate, exported a before PDF, saved a new PPTX, closed, reopened,
re-read the exact two-slide text inventory in order, and exported an after PDF.
The source bytes stayed unchanged. Both corresponding 1600 × 900 page pairs
were byte-identical, and one independent pixel-only review of anonymous frozen
sets passed with no finding. The raw files, transcript, pixels, and review remain
ignored local evidence. This closes only that exact compatibility fact; it does
not broaden the grammar, prove the full 2–11 range, authorize target-specific
native/OMML ordered delivery, or change support. Eight focused adapter tests and
the complete 998/998 suite pass with this evidence boundary in place.

The next bounded slice closes one exact mixed ordered final path without adding
an authority layer. A complete readable project tuple drives one atomic batch
for `clone/fill → typed OMML → native card-arrow`; all three source artifacts,
IR/plan/evidence tuples, ordered bytes, and CandidateBuildRecord facts are
regenerated and exact-compared before receipts are consulted. The existing
`QaReport` and `BuildArtifact` publisher remains the only delivery boundary.
Role swaps, readable story/LaTeX drift, late preflight failure, incomplete
evidence, confused compatibility purpose, and post-await caller mutation fail
closed.

That exact mixed public-synthetic candidate passed PowerPoint 16.111.3
open/save/reopen using one stable ignored carrier file in the existing evidence
directory. The carrier preserves device/inode identity while replacing and
verifying exact bytes, avoiding repeated macOS file consent; it is a deliberately
non-atomic manual transport, not product publication. Formula compatibility now
uses `mc:AlternateContent` with a native
`a14:m → m:oMathPara → m:oMath` choice and editable linear-text fallback. After
raising the fraction to 48 pt in an expanded semantic slot, all three 1600 × 900
before/after page pairs are byte-identical and the independent pixel review
passes. The reviewer initially reported missing text from a transient incomplete
image display, but exact-byte equality and exact-path replay disproved it; both
visual-review prompts now require a repeat view before such a finding, while
`prepareVisualEvidencePlan` owns equality/drift facts and emits the minimal
review-image set from bounded detached bytes. The complete repository suite
passes 1021/1021 at this boundary. Its admission boundary reads only captured
TypedArray/ArrayBuffer internal-slot getters, rejects forgeable metadata,
shared/resizable backing stores, and copies immediately after pre-copy limits.
The bounded closing re-review found no blocker, high, or medium issue.

D-031 now closes the complete same-candidate evidence and publication chain for
those exact fixed mixed bytes. The public evidence module can create and inspect
the readable manifest but returns only frozen `authority: none` facts; it cannot
mint a core receipt. The fixed explicit-trust local CLI rereads inputs through
bounded `O_NOFOLLOW` handles, snapshots captured PDFs, requires exact page counts,
rerasterizes all three pages with explicit Poppler tools, exact-compares every
PNG, and only then creates private same-process one-shot receipts. The existing
mixed final preparer regenerates the full project/candidate relation and the
shared publisher links `BuildArtifact` last. A fresh local finalize reproduced
candidate `ccbffcb1…2802`, manifest `5f9b0c23…1a42`, and render set
`render-98fd2a63…f9ab`. The bounded review's blocker, high, and medium are all
closed; final re-review reports 0/0/0 and the complete suite passes 1032/1032.

D-032 now captures the manual formula-edit boundary without laundering it into
delivery authority. The edited derivative is frozen locally at
`2592ad9d…8d3`; the original fixed mixed candidate and record remain
`ccbffcb1…2802` and `5a88c576…15c1`. A pure public inspector verifies the
record and reports only the observed `3/3 -> 2/3` fraction relation plus narrow
namespace counts. It has no gate or pass status and explicitly states that a
closed OMML grammar was not run. The fixed trusted-local recorder alone pins
all three full digests, the exact source/slide relation, and the readable edit,
then places the user's statement under `operatorAttestation`; it returns
`authority: none` and cannot create the direct single-slide receipt. Alternate
candidates, unrelated-slide plus unknown-part tampering, and unknown math nodes
are covered by 12 focused tests. The complete suite passes 1044/1044, and final
re-review reports 0 blocker and 0 high. One accepted trusted-local Medium
remains because Node.js has no `openat` equivalent for the final `wx` pathname
create; post-write directory identity checks detect but cannot prevent a
same-principal directory swap. The stable PowerPoint carrier has been restored
in place to the exact original candidate, while the edited derivative remains
ignored. The operator explicitly confirmed that both opens occurred without
repair or conversion prompts, so the fixed recorder created a passing feature-
level `COMPAT-OMML-001` record. That result remains non-authorizing and does not
create the exact direct-candidate receipt.

The fixed mixed final-delivery path now directly exercises the shared
publication fault contract instead of relying only on the direct-card tests.
A cancellation after publication starts but before the `BuildArtifact` link
returns `not-committed` with `rollbackStatus: complete` and leaves the output
root empty. A failure observed immediately after the `BuildArtifact` link
returns `commit-uncertain`, attempts no destructive rollback, and retains the
candidate record, QA report, PPTX, and build marker. The focused mixed suite now
passes 13/13, and the complete repository suite passes 1046/1046. No mixed-only
publisher or fault branch was added.

D-029 fixes the authority map before that work continues. The source template
owns unchanged inherited content/structure; raw brief, evidence, and `DeckSpec`
own authored semantic content and relationships; `SlideLayoutIR` owns editable
layout constraints; `ComposedSlidePlan` owns deterministic geometry and measured
receipts. `CandidateBuildRecord` is layout/build replay authority, not generation
authentication or a complete semantic source. Existing hashes are exact-object
boundary checks only. Do not add `GenerationIntent`, a persisted formula AST,
or a new delivery root; reuse `QaReport` and `BuildArtifact`, with same-artifact
pixels mandatory at final acceptance.

The first borrowed-mechanism vertical slice is now executable.
`labs/layout-selection/reviewed-clone-fill-catalog.mjs` binds explicit reviewed
roles and capacities to exact synthetic source shapes, selects deterministically,
and atomically hands its private frozen plan to M2-005A; eight focused tests pass.
An exact action-first candidate and generic evidence-first control rendered at
1600 × 902. Three isolated reviewers agreed that the exact hierarchy serves the
approval goal better; one also identified incomplete decision evidence and
small supporting type. The aggregator returned `pass` because those findings
had only one-run support, but the dissent remains an explicit next constraint.
No render, raw review report, or PPTX was committed.

The multi-exemplar profile slice is also executable.
`labs/layout-selection/reviewed-profile-induction.mjs` securely snapshots three
different runtime-generated synthetic POTX sources, emits wording- and
font-name-redacted geometry/typography/salience proposals, requires one frozen
hash-bound accepted-or-rejected decision per proposal, and selects the winning
exact source before atomic assembly. The review accepts clear 44/20 pt decision
and 48/24 pt status hierarchies and rejects an equal 28/28 pt hierarchy.
Minimum 24/18 pt type, one takeaway, one evidence unit, inferred capacity,
pre-copy archive size, and bounded index-capture gates are executable; ten
focused tests pass. Two real 1600 × 902 renders were judged goal-met by all
three isolated reviewers and aggregated to `pass`; a contradictory one-run
composition minor was dismissed by a bounded pixel-location follow-up. See
`docs/M2-005_PROFILE_INDUCTION.md`. No binary or raw review output was committed.

A second deck-level review of Anthropic's public PPTX and frontend-design
instructions, ppt-master, the AWS spec-driven maker, Microsoft Narrative
Builder, PPTAgent/DeepPresenter, Presenton, Gamma, Beautiful.ai, Canva,
PresentBench, and the current Codex presentation workflow found that the prior
brief-to-layout correction was still one layer too low. The fundamental missing
artifact is a frozen, testable deck hypothesis that joins one audience change,
a causal narrative, and a subject-derived visual language before layout
selection. Public example contact sheets also showed that distinctiveness must
be conditioned on live-versus-reader delivery, not maximized blindly. The
clean-room evidence, self-critique, contract, and revised sequence are in
`docs/PRESENTATION_DESIGN_PLANNING_REVIEW.md`; no external prompt, asset, sample
deck, render, or history entered Git.

The bounded deck-level challenge then found one blocker, four high, and two
medium gaps: validation without an executable planner, circular contract-aware
review, a confounded A/B, missing template precedence, public-scope governance,
votable required evidence, and incomplete evidence pins. The final plan now
requires raw-input→candidate→selection→deck execution, a blind outcome probe,
controlled and randomized comparisons, locked/flexible/scratch template modes,
mandatory required-item reconciliation, fixed sources, and D-023 governance.
Stable-snapshot closure found no remaining blocker, high, or medium issue.

GitHub now contains the first public `main` ref for
`why7682/pptx-compiler`. The exact published object is
`410642b668514ab7193212d617cc0f8acb837924`. Immediately before transport,
the final history scan covered 14 commits, 505 unique leaf-entry versions,
7,698,368 regular-blob bytes, and 28 author/committer occurrences with zero
finding. Repository ID `1330979133`, PVR `enabled: true`, and exact remote-main
equality were reverified after the push. This completes M3-006; it is source
publication, not an npm package or release.

M3-004B remains partial. The first hosted run passed Ubuntu and macOS under
Node 22/24 and passed CodeQL, but failed both Windows cells. Pull-request run
`31582316951` again passed the four Ubuntu/macOS cells while exposing two
remaining Windows boundaries: Git for Windows needs Git's `/dev/null` pathname
instead of Node's Win32 null-device spelling, and npm did not raise the tar mode
for a nested bin target. The current correction uses one Git-native null path,
one package-root CLI entry, and a tip-owned schema-v2 policy that binds the
retired executable only by exact historical path plus blob OID; current/index/
working-tree admission remains limited to the new path. Independent code review
reports 0 blocker, 0 high, and 0 medium. In the fixed non-FileProvider copy,
Node 22.23.2 and Node 24.19.0 each pass the 216-node affected focus, the
24-node package-stage suite, and the 1228-node complete suite. npm 10.9.8 and
npm 11.17.0 each build, admit, install offline, and smoke all four packages;
the package-root CLI member is mode 0755. Pull-request Public CI run
for branch object `94b5c1c` then passed all six Ubuntu/macOS/Windows × Node 22/24 cells,
including complete tests, guarded packages, working-tree recheck, and drift
check. After public visibility and the dependency graph were enabled,
pull-request security run `31594128139` passed Dependency Review. Those hosted
results are historical evidence bound to branch object `94b5c1c`; the next
committed evidence/document snapshot requires fresh pull-request CI and
Dependency Review before merge. Accepted-main CI/CodeQL remain required after
merge. The four npm-name checks remain time-bounded and reserve nothing, and
npm publication is separately blocked.

## Persistent presentation-skill boundary

This boundary survives compaction because this file and `AGENTS.md` are on the
mandatory startup path:

- Presentation Design owns upstream audience change, causal narrative,
  per-slide communication jobs, evidence chain, and whole-deck rhythm.
- Guizang PPT skill is a midstream source of testable visual grammar and failure
  experience only. Its HTML/CSS/WebGL runtime, templates, assets, fixed theme,
  quotas, and Swiss style are not imported or made normative.
- This repository alone owns raw-input capture, candidate planning, template
  matching, PPTX assembly, same-path rendering, blind randomization, evidence,
  mutation authority, and support status.
- Verification agents see frozen anonymous pixels and a review context only;
  they neither generate nor mutate, know no hidden factor/answer, and cannot
  approve their own repair.
- Presentation file skills assist only with PPTX mechanics and visual inspection
  methods; repository contracts and generators remain authoritative.

## Immediate objective

The completed M2-005 boundary includes typed native and OMML application,
semantic-slot composition plan, collision gate, non-deliverable candidate
writer, PowerPoint edit/save/reopen probe, authenticated ordered bridge, and
direct receipt-bound semantic-card, source-preserving typed-OMML, bounded
clone/fill-only ordered, and fixed three-slide mixed ordered final-delivery
implementations now pass. Generic raw-byte delivery remains unavailable; typed
OMML cannot pass its direct final compatibility gate until an exact direct-
candidate receipt exists. Both the fixed two-slide
clone/fill candidate and fixed mixed candidate now have actual
`ordered-deck-open-save-reopen` evidence. Broader ordered/template compatibility,
broader fault evidence, and the exact direct-OMML candidate receipt remain open.
The frozen mixed-candidate edit observation has a passing feature-level record
only. The
complete same-candidate mechanical/render/pixel/compatibility/QA publication
chain is closed only for the exact fixed mixed candidate described by D-031.
The public
synthetic match-to-render, multi-exemplar profile, bounded ordered-story graph,
and P1 executable planning lab are complete. `labs/design-planning/` now derives
three complete hypotheses from one raw brief/evidence/template bundle, returns
only a non-authorizing recommendation without review, binds one external
selection plus both rejected alternatives to a separately reviewed approval
record whose complete canonical digest is fixed in the planner's reviewed
registry, and projects a module-private receipt token through the existing
reviewed layout and ordered assembly path. The template claim is bound to the
actual proposal-set and layout-acceptance hashes; the final assembly receipt
also binds the story projection and PPTX hash. Reviewer `0.2.0` and its fixed
P1 atomic outcome binding are implemented. P3's controlled matrix, real local
renders, three blind reviews, common-root repair, repaired native round trip,
typed formula target, and direct-candidate serialized build record are complete.
The normative record now also covers complete generic, semantic-card, and typed-
OMML ordered decks. D-029 narrows it to layout/build replay instead of full semantic
authority. The direct single-slide semantic-card delivery slice now reuses
`QaReport` and `BuildArtifact` after exact target-specific deterministic
source/DeckSpec/plan projection, with BuildArtifact-last publication,
pre-commit cancellation rollback, and post-marker uncertainty. The fixed mixed
path now directly covers both commit fault states; broader target-specific
ordered/OMML delivery remains a later unavailable capability. The source-facts →
reviewed semantic profile → deterministic selection → complete-batch atomic
assembly boundary remains unchanged.

D-033 freezes that as the M2-005 completion boundary. The exact fixed mixed
public-synthetic candidate satisfies source immutability, replayable IR/plan,
deterministic candidate/record output, complete allowed/collateral diff, same-
artifact render and pixel review, ordered PowerPoint compatibility,
QaReport/BuildArtifact-last publication, and both commit fault states. The exact
direct single-slide OMML receipt and generalized target-specific native/OMML
ordering remain unavailable follow-on capabilities; they do not reopen the
assembly milestone or change support. The bounded exit review found 0 blocker,
0 high, and 0 medium. The next dependency is `M3-001`.

D-034 starts M3-001 with one thin CLI authority boundary. Protocol `0.1.0`
uses only exit meanings `0/1/2`; JSON mode emits exactly one deterministic
stdout envelope on success or failure and copies only a safe error code and
pointer. Real `inspect` and `validate` commands now reuse the normative Schema,
`ProjectContext`, and secure-ingestion code. `inspect` may create only the
configured TemplateIndex path; `validate` is read-only and creates no dispatch
plan. Six remaining M3 commands fail explicitly as unavailable. Ten focused
process tests pass, including fatal UTF-8 rejection and post-link commit
uncertainty. The bounded re-review reports 0 blocker, 0 high, and 0 medium.
This is not a renderer, release, or support claim; M3-001
remains in progress.

D-035 freezes renderer work and corrects the M3 critical path after a five-layer
Linus audit plus one bounded independent challenge. The old plan had three
blockers: no complete render path is authorized by the current support matrix;
headless QA lacked an explicit honest outcome when visual/compatibility evidence
is absent; and public CI was ordered before creation of the public repository.
The first release is now a candidate alpha. It may produce candidate PPTX plus
CandidateBuildRecord, but missing required evidence yields the existing
QaReport with `decision: blocked` and no BuildArtifact. `supportClaimsEnabled`
remains false.

The next implementation slice is the on-disk project spine, not render. Add one
create-only public-synthetic bootstrap under one selected root; remove redundant
`inspect --output` before publication; retain the current single-document Schema
check only under an explicit document meaning; and implement whole-project
validation against the real static host and normative support policy with no
project invocation execution or output. Generic onboarding, doctor, standalone
diff, and formula CLI do not block the candidate alpha. The complete correction is durable in
`docs/M3_LINUS_AUDIT.md` and D-035.

D-036 implements that project spine. `init --preset
public-synthetic-native-card` creates one schema-valid project beneath an
explicit new root through the production `packages/public-synthetic` boundary.
ProjectConfig is the visible commit marker; pre-marker failures either complete
an exact owned-path rollback or report rollback-incomplete, while every failure
after marker visibility reports commit-uncertain and preserves reconciliation
state. `inspect` now derives its only output from ProjectConfig. `validate
document` retains the narrow Schema diagnostic, and `validate project` loads all
six readable authorities, re-inspects the source, exact-compares the index,
admits the fixed static registration through its installation conformance
fixtures, resolves the normative matrix, and prepares then discards the complete
project plan. Machine output distinguishes registration conformance execution
from the project invocation that was not run and grants no render eligibility.
Nineteen CLI tests, 263 focused tests, and the complete 1065-test suite pass.
The bounded review's two high and one medium findings were fixed and rechecked
with 0 blocker, 0 high, and 0 medium remaining. No support row changed.

D-037 implements the next exact candidate-only render slice. The frozen
`public-synthetic-native-card-candidate` profile owns 15 invariant support rows,
requires the executing Linux/macOS/Windows Node 22/24 row plus
`automated-public-synthetic`, and rejects the complete operation before OOXML
mutation if any row is missing or not `experimental/accept-with-warning`.
`supportClaimsEnabled` remains false. One stable source read now returns both
detached bytes and the fresh TemplateIndex; the full readable index graph is
exact-compared with the installation-owned public-synthetic golden before the
candidate profile is authorized.

`render` has no output override. It re-derives the semantic
`slide-content-tail` placement, exact-compares the complete geometry with
DeckSpec, prepares a fresh one-shot project plan, executes one typed native-card
invocation, and publishes only `<deckId>.candidate.json` plus `<deckId>.pptx`
under ProjectConfig's output root. The file-flushed record precedes the PPTX
logical commit marker, and injected failures distinguish not-committed,
rollback-incomplete,
and commit-uncertain states. The result is labeled experimental and
`deliveryEligible=false`; no QaReport or BuildArtifact is created. The focused
D-037 set passes 180/180 and the complete suite passes 1076/1076 on Node 22.
The bounded code review found one medium false dependency boundary: the CLI
transitively loaded the broad OMML/ordered publisher. A native-card-only entry,
one shared format-independent pair state machine, and a recursive import-graph
regression closed it; re-review reports 0 blocker, 0 high, and 0 medium.
The frozen 1600 x 902 candidate render
passed one independent pixel-only review with no finding; that is visual
evidence, not self-authorization or delivery approval.

D-038 implements honest QA over only that exact candidate pair. `qa` reloads
the current readable project graph, repeats the same semantic-slot and fresh
one-shot derivation as `render`, reconstructs the authenticated expected PPTX
and canonical CandidateBuildRecord, then exact-compares both stable-read actual
files and verifies record replay. The resulting existing `QaReport 0.1.0`
records three internal structural passes and marks pixel review, PowerPoint
compatibility, and render completeness unavailable. The command succeeds with
`decision=blocked`, persists and returns the same complete report, creates no
BuildArtifact, and cannot accept or discover ignored local evidence. Missing,
tampered, oversized, or stale pairs fail without a report; repeat QA preserves
the first report through create-only conflict semantics. Support counts and the
false global claim switch remain unchanged.

D-038 closes with 33 CLI tests, a 99/99 focused run, a 1085/1085 complete
Node 22 strict run, and a 0 blocker / 0 high / 0 medium bounded re-review.
Forbidden-material and provenance gates each pass across 216 working-tree
files; the 60-row support gate and contract gate also pass. M3-001 is complete,
and D-039 now completes the next package-boundary definition.

D-039 freezes the candidate-alpha installation graph at exactly four packages:
CLI, core, native-card-arrow, and public-synthetic. One positive package plan
owns their exact source-to-stage mappings, exports, types, bin, Node 22/24
engines, MIT metadata, import aliases, internal edges, and package-owned static
resources. The installed composition no longer treats the monorepo root as a
runtime resource root; the old explicit-root CLI remains compatible. The four
unscoped names returned E404 from the official npm registry on 2026-08-11, but
that reserves nothing. D-045 now binds the created GitHub repository while npm
publication remains blocked.

The package/CLI/native/policy focus passes 146/146 and the complete Node 22
strict suite passes 1107/1107. Package-plan, forbidden-material, provenance,
support-matrix, contract, and diff gates pass; the first two tree gates each
cover 239 admitted files. The bounded review's one high fixed-profile gap and
one medium hidden-loader gap were fixed, and final re-review reports 0 blocker,
0 high, and 0 medium. That remains package-definition evidence only.

D-040 completes M3-003's local package gate. One retained snapshot owns the
readable plan, forbidden-material policy, `.gitignore`, and exact mapped source
bytes/modes. Npm-private leaf manifests are derived from that snapshot; D-045
adds their exact repository projection without granting npm publication. The same
admitted Node runtime invokes its bundled npm offline with scripts and ambient
configuration disabled; an independent narrow canonical gzip/ustar parser,
not npm's report, admits the exact planned members and limits.

All four retained tgz buffers install together in an empty directory. Exact
installed package and `.bin` inventories pass, and the installed CLI executes
`init -> inspect -> validate project -> render -> qa` from outside the source
workspace. QA remains blocked and no `BuildArtifact` exists. The one ignored
`.package-stage/` uses atomic ownership/claim handling, marker-authenticated
`work/reviewed/previous/failed` recovery, full-evidence completion checks, and
retains only the marker, canonical evidence, and four tgz files. Recovery has no
recursive deletion primitive: exact transient inventories or marker-proved
review members are removed explicitly, while foreign attempts move only by
rename into a persistent quarantine. The pre-marker interruption window
restores last-good `previous` and preserves the new candidate in `failed`.
Focused tests pass 22/22, the complete Node 22.23.2 strict suite passes
1129/1129, and the actual build passes with npm 10.9.8. Closing review reports
no remaining blocker or high finding. Public CI, repository metadata, and
publication evidence were still absent, making M3-004A the next dependency at
that checkpoint.

D-041 completes M3-004A's local workflow contract without pretending that local
YAML is public CI evidence. Exactly two byte-locked workflows define a fixed
Linux/Windows/macOS × Node 22/24 matrix, least privileges, complete action SHAs,
CodeQL `build-mode: none`, and ordinary-PR dependency review. One exact
TypeScript 6.0.2 dev tool checks a plan-derived eight-subpath declaration
consumer. Stable-source syntax policy, exact package scripts, mechanical MIT/
provenance relations, and a deterministic four-package CycloneDX static-ESM SBOM projection
close the local development boundary. All admitted platforms file-flush owned
payloads; POSIX additionally requests directory fsync barriers, while Windows
makes no sudden-power-loss directory-entry persistence claim. Public six-cell,
CodeQL, and Dependency Review evidence remains M3-004B after M3-006. M3-005 is
now split: M3-005A completes the local security/contribution/governance/
reproduction contract, while M3-005B remains blocked on public evidence.
At the D-045 checkpoint, GitHub private vulnerability reporting was verified
active on the then-empty public repository. D-045 bound its canonical identity
into package-plan schema v2, the leaf manifests, and the root SBOM component.
M3-006 later completed the final local commit, history scan, exact-OID transport,
and post-push remote/PVR verification.

D-042 completes M3-005A without publishing a personal contact or pretending a
document activates a remote setting. `SECURITY.md`, `CONTRIBUTING.md`,
`GOVERNANCE.md`, and `docs/REPRODUCIBILITY.md` each own one human process; text
and copyright leakage rules admit no path exception. The focused policy and
document suites pass 41/41 and 14/14. In the fixed non-FileProvider copy, both
Node 22.23.2/npm 10.9.8 and Node 24.19.0/npm 11.17.0 pass 1198/1198 complete
tests, the 124-file source gate, and the real four-package offline install plus
installed CLI smoke. At that checkpoint this was still local evidence: no
public repository, active private-reporting setting, hosted CI/security result,
or publication was claimed. D-045 later created the empty shell and verified
PVR; M3-006 later published the exact first source ref. The final
bounded M3-005A reviews report 0 blocker, 0 high, and 0
medium findings.

D-043 defined the then-local first-public-ref boundary. The existing
forbidden-material scanner now has a bounded `history` mode that reads the
policy from `refs/heads/main`, walks the tip's raw parent/tree OIDs, scans every
reachable tree-entry version and commit message, and separately exact-checks
author/committer metadata against
the repository-local public Git identity without printing it. Deleted
forbidden files, message leakage, identity drift, symlinks, oversized blobs,
shallow/overridden history, and a moving main ref fail closed. At the D-043
preflight checkpoint, the 13-commit history scanned 286 unique leaf-entry versions, 3,677,606 regular-blob
bytes, and 26 identity occurrences with zero finding; the focused policy suite
passes 49/49. Raw commit parent/tree OIDs, not mutable shallow reachability,
own ancestry; repository indirection, control-path injection, noncanonical tree
grammar, and fsck warnings fail closed without echoing private path/identity
bytes.
Bounded independent closing reviews reproduce the environment, shallow/merge,
raw-tree, fsck-configuration, and redaction probes and close D-043 at 0 blocker,
0 high, and 0 medium.
That was preflight only. M3-006 later rescanned the final 14-commit history,
pushed the exact reviewed object ID, and verified remote-main equality plus the
repository/PVR tuple.

D-044 now fixes the final pre-public identity at
`why7682/pptx-compiler`. The CLI, four npm package names, workspace/package
imports, default project configuration filename, CycloneDX projection, and
unpublished serialized-contract namespace use the one `pptx-compiler` stem.
There is no old-name compatibility alias. The local folder is unchanged, and
the old stem remains only in dated decision/query history plus the existing
ignored package-stage owner/plan ID and transaction prefixes needed for crash
recovery. In the fixed non-FileProvider copy, Node 22.23.2 and Node 24.19.0 each
pass 1206/1206 tests and their npm 10.9.8/11.17.0 package builds each admit four
renamed tarballs, install them together offline, execute the installed CLI
spine, return blocked QA, and create no BuildArtifact. Two bounded current-byte
closing reviews report 0 blocker, 0 high, and 0 medium; D-044 provenance is
closed.

D-045 closed the repository and local-package half of M3-006. At that
checkpoint GitHub reported the empty public repository as ID `1330979133` and
PVR as exact `enabled: true`; no local remote or source ref existed. Package-plan schema v2 owns the exact GitHub
provider/ID/owner/name/URL tuple. Leaf repository objects and the root SBOM VCS
reference are pure projections, while `private: true` plus
`npm-publication-not-authorized` keeps npm closed. Package evidence/marker v2
binds the canonical readable plan fingerprint so old tarballs cannot satisfy a
new repository plan. Node 22.23.2 and Node 24.19.0 each pass the 1217-node
complete suite; the 24-node package-stage suite and real npm 10.9.8/11.17.0
four-tarball offline installed-CLI builds also passed. M3-006 subsequently
completed the final 14-commit history scan, canonical HTTPS transport,
exact-object-ID first push, remote-main equality check, and post-push
repository/PVR verification.

## First commands after switching workspace

```sh
git status --short --branch
git config --local --list
rg -n '/[U]sers/|[A-Za-z]:\\\\[U]sers\\\\|PRIVATE KEY|BEGIN [A-Z ]+ PRIVATE KEY' .
rg --files
git log -1 --oneline
git remote -v
```

Select an admitted Node 22 or 24 runtime first, then follow the exact sequence
in `docs/REPRODUCIBILITY.md`. Package scripts and the canonical public workflow
remain the machine command authority.

## Next work session

1. Read `docs/M3-004_HANDOFF.md`. PR #1's six-cell CI and Dependency Review
   passed for historical branch object `94b5c1c`. Retain fresh checks for the
   next committed PR head before merge, then retain final main-run CI and
   CodeQL evidence before promoting M3-004B.
2. Keep final release/platform wording in M3-005B until M3-004B closes.
   Apply D-046: final reader-facing documents must construct the system from
   purpose and fact ownership through flow, contracts, evidence, limitations,
   and next action; chronology stays in handoffs/decisions/provenance.
3. Keep predecessor code, presentation assets, converter output, and generated
   archives out of Git; use only repository-owned text/synthetic fixtures for
   public tests.

## Known predecessor findings

These facts justify the clean extraction boundary; they are not evidence that
this repository already implements the corresponding functions.

- The predecessor contains useful template inspection, formula, OOXML QA,
  capability-registry, and deterministic build experiments.
- Several apparent generic paths still dispatch to project-specific renderers,
  fixed slide/shape identifiers, or helpers that cannot be redistributed.
- A package dry-run without a positive allowlist produced a 1,440,242,285-byte
  tarball and 1,751,118,178 bytes unpacked, including build and worktree data.
  No package was published.
- Structural onboarding checks have been demonstrated on two fixtures, but a
  fully generic render has not. Do not claim arbitrary-template support.
- Direct formula-to-OMML target application has automated structural evidence,
  a narrow PowerPoint open/save/reopen probe, and one frozen mixed-candidate
  human `3/3 -> 2/3` edit/save/close/reopen observation. The operator explicitly
  attested to no repair/conversion prompt and the exact local feature record
  passed; it cannot substitute for a direct-candidate receipt.

## Private local compatibility testing

Private files may be used from outside this repository under
`docs/PRIVATE_FIXTURE_POLICY.md`. Configure paths only in an ignored `.env` or a
local manifest. Never copy the input, derived preview, extracted XML, filename,
path, embedded metadata, slide text, or screenshot into this Git tree or public
CI artifacts.

## Definition of the first releasable alpha

A candidate alpha is eligible only when a clean checkout can install the packed
tarball and run the documented public-synthetic bootstrap, inspect,
whole-project validate, candidate-only render, and QA flow. The candidate must
pass archive/relationship/schema/collateral checks and bind an exact
CandidateBuildRecord. When public headless evidence is incomplete, QA must
return a schema-valid blocked QaReport and no BuildArtifact. Package/repository
leakage gates and public three-platform CI must pass; every exercised
experimental capability has real registration/conformance evidence and explicit
opt-in; `supportClaimsEnabled` remains false; and compatibility limits are
documented. A delivery alpha requires the missing evidence and remains later.
